{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File as Module (Explicit Path)",
            "type": "python",
            "request": "launch",
            "module": "${fileDirnameBasename}.${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env",
            "args": [
                // You can add default arguments here if needed
            ]
        }
    ]
}